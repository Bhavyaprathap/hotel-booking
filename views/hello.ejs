<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>3D Carousel with Hostel Reviews</title>
    <link rel="stylesheet" href="styles.css">
    <img src="https://via.placeholder.com/300x200?text=Luxury+Room" alt="Luxury Room">

    <style>
        body {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: linear-gradient(135deg, #007BFF, #43cbb9);
            margin: 0;
            color: white;
        }
        
        h1 {
            font-size: 3rem;
            margin-bottom: 20px;
        }

        .carousel {
            margin-top: 100px;
            perspective: 1000px;
            margin-bottom: 50px;
        }

        .carousel-inner {
            position: relative;
            width: 300px;
            height: 300px;
            transform-style: preserve-3d;
            transition: transform 1s;
        }

        .carousel-item {
            position: absolute;
            width: 100%;
            height: 100%;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            backface-visibility: hidden;
            text-align: center;
        }

        .carousel-item img {
            width: 100%;
            height: 100%;
            border-radius: 10px;
        }

        .text {
            position: absolute;
            bottom: 10px;
            width: 100%;
            text-align: center;
            background-color: rgba(0, 0, 0, 0.6);
            padding: 5px;
            border-radius: 0 0 10px 10px;
            font-size: 1.2rem;
        }

        .carousel-item:nth-child(1) { transform: rotateY(0deg) translateZ(150px); }
        .carousel-item:nth-child(2) { transform: rotateY(120deg) translateZ(150px); }
        .carousel-item:nth-child(3) { transform: rotateY(240deg) translateZ(150px); }

        button {
            position: absolute;
            top: 50%;
            transform: translateY(-50%);
            background-color: rgba(0, 0, 0, 0.5);
            color: white;
            border: none;
            padding: 10px;
            cursor: pointer;
        }

        .prev {
            left: -50px;
        }

        .next {
            right: -50px;
        }

        .reviews-section {
            width: 80%;
            margin-top: 50px;
            text-align: center;
        }

        .review {
            background-color: rgba(0, 0, 0, 0.7);
            margin: 10px;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0px 5px 10px rgba(0, 0, 0, 0.2);
        }

        .cost-section {
            margin-top: 30px;
            font-size: 1.5rem;
            font-weight: bold;
            background-color: rgba(0, 0, 0, 0.7);
            padding: 15px;
            border-radius: 10px;
        }

        /* Animation on hover */
        .carousel-item:hover .text {
            background-color: rgba(0, 0, 0, 0.8);
            transform: translateY(-10px);
        }

    </style>
</head>
<body>

    <h1>Explore Our Rooms</h1>

    <!-- 3D Carousel -->
    <div class="carousel">
        <div class="carousel-inner">
            <div class="carousel-item">
                <img src="https://via.placeholder.com/300x200?text=Room+1" alt="Image 1">
                <div class="text">Luxury Room</div>
            </div>
            <div class="carousel-item">
                <img src="https://via.placeholder.com/300x200?text=Room+2" alt="Image 2">
                <div class="text">Cozy Stay</div>
            </div>
            <div class="carousel-item">
                <img src="https://via.placeholder.com/300x200?text=Room+3" alt="Image 3">
                <div class="text">Budget Room</div>
            </div>
        </div>
        <button class="prev" onclick="rotate(-1)">&#10094;</button>
        <button class="next" onclick="rotate(1)">&#10095;</button>
    </div>

    <!-- Hostel Reviews Section -->
    <div class="reviews-section">
        <h2>What Our Guests Say</h2>
        <div class="review">
            <p>"Amazing experience! The room was clean and comfortable, and the staff was very friendly." - Bhavya</p>
        </div>
        <div class="review">
            <p>"Great value for money! Loved the location and the amenities." -Prashanth</p>
        </div>
        <div class="review">
            <p>"Perfect for a weekend getaway. The view from the room was spectacular!" - Prashanth2</p>
        </div>
    </div>

    <!-- Cost Section -->
    <div class="cost-section">
        <h3>Room Prices</h3>
        <p>Any room:1000/night</p>
        
    </div>

    <script>
        let currentIndex = 0;
        const items = document.querySelectorAll('.carousel-item');

        function rotate(direction) {
            currentIndex += direction;
            if (currentIndex < 0) {
                currentIndex = items.length - 1;
            } else if (currentIndex >= items.length) {
                currentIndex = 0;
            }
            const angle = currentIndex * -120; // 360 / number of items
            document.querySelector('.carousel-inner').style.transform = `rotateY(${angle}deg)`;
        }
    </script>

</body>
</html>
